import 'package:flutter/material.dart';

class DificilPage extends StatelessWidget {
  const DificilPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Dificil Page');
  }
}
